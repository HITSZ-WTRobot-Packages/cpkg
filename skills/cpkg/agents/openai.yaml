interface:
  display_name: "cpkg"
  short_description: "Manage WTR projects, packages, and cpkg updates"
  default_prompt: "Use $cpkg to manage a WTR project, driver package, global configuration, or update the installed cpkg CLI from official GitHub Releases."
